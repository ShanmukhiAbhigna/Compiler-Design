#include<stdio.h> 
#include<conio.h> 
#include<string.h> 
char input[100];
int i,l;
int E(),EP(),T(),TP();
int main()
{
 	printf("Recursive descent parses");
 	printf("Gramme is:E->TE'\nE'->TE'|@\nT->FT'\nT'->*FT'|@\nF->(E)|id");
 	printf("Enter string to be chected");
 	gets(input);
 	if(E())
 	{
 		if(input [i]=='\0')
 		
 			printf("String is accepted");
		
		else
		
			printf("Not accepted");
		
	}
	else 
        printf("\nString not accepted"); 
        getch();
    }
	E()
	{
		if(T())
		{
			if(EP())
			
				return (1);
			
			else
				return (0);
			
		}
		else	
		   return(0);
	}
	EP()
	{
		if(input[i]=='+')
		{
			i++;
			if(T())
			{
				if(EP())
				{
					return (1);
				}
				else
				return (0);
			}
			else 
			return (0);
		}
		else 
		return (0);
	}
	F()
	{
		if(F())
		{
			if(TP())
			{
				return i;
			}
			else
			return 0;
		}
		else 
		return 0;
	}
	TP()
	{
		if(input[i]=='*')
		{
			i++;
			if(F())
			{
				if(TP())
				{
					return 1;
				}
				else 
				return 0;
			}
			else
			return 0;
		}
		else
		return 0;
	}
else if(input[i]>='a'&&input[i]<='z' || input[i]>='A'&&input[i]<='Z')
{
	i++;
		return (1);
	}
	else
	return (0);
}
